# Interactive Score — Waiting in a Line

Open `index.html` in a browser. Hover any note symbol to hear rhythmic 20Hz sine pulses using p5.js sound.

Notes:
- Uses p5.js and p5.sound via CDN.
- If audio doesn't play on first hover, click/tap on the page to allow audio context.
